# --- AI providers (the only keys the pipeline uses) ---
ANTHROPIC_API_KEY=your_anthropic_api_key
GROQ_API_KEY=your_groq_api_key
# gpt-image-2 - generates the wolf background images (video + slides).
OPENAI_API_KEY=your_openai_api_key
